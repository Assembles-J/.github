name: Reusable Android Build

on:
  workflow_call:
    inputs:
      java-version:
        description: JDK version used by Gradle
        required: false
        type: string
        default: '21'
      gradle-task:
        description: Gradle task to run
        required: false
        type: string
        default: assembleDebug
      working-directory:
        description: Directory containing the Gradle project
        required: false
        type: string
        default: .
      artifact-name:
        description: Optional artifact name. Leave empty to skip upload.
        required: false
        type: string
        default: ''
      artifact-path:
        description: Artifact path relative to repository root.
        required: false
        type: string
        default: ''
      setup-android:
        description: Install/configure Android SDK before running Gradle.
        required: false
        type: boolean
        default: true
      use-wrapper:
        description: Use ./gradlew when the repository already contains a Gradle wrapper.
        required: false
        type: boolean
        default: true
      gradle-version:
        description: Standalone Gradle version used only when use-wrapper is false.
        required: false
        type: string
        default: '8.10.2'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ inputs.java-version }}

      - name: Set up Android SDK
        if: inputs.setup-android
        uses: android-actions/setup-android@v3

      - name: Set up Gradle wrapper cache
        if: inputs.use-wrapper
        uses: gradle/actions/setup-gradle@v4

      - name: Set up standalone Gradle
        if: ${{ !inputs.use-wrapper }}
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: ${{ inputs.gradle-version }}

      - name: Grant Gradle wrapper permission
        if: inputs.use-wrapper
        working-directory: ${{ inputs.working-directory }}
        run: chmod +x gradlew

      - name: Build with wrapper
        if: inputs.use-wrapper
        working-directory: ${{ inputs.working-directory }}
        run: ./gradlew --no-daemon ${{ inputs.gradle-task }}

      - name: Build with standalone Gradle
        if: ${{ !inputs.use-wrapper }}
        working-directory: ${{ inputs.working-directory }}
        run: gradle --no-daemon ${{ inputs.gradle-task }}

      - name: Upload build artifact
        if: inputs.artifact-name != '' && inputs.artifact-path != ''
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact-name }}
          path: ${{ inputs.artifact-path }}
          if-no-files-found: error
